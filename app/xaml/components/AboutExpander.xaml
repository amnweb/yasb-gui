<Expander xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         HorizontalAlignment="Stretch"
         HorizontalContentAlignment="Stretch"
         Padding="16,0,16,16"
         Margin="0,24,0,0">
    <Expander.Header>
        <Grid Margin="0,16,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Image x:Name="IconImage" Grid.Column="0" Width="36" Height="36" Margin="0,0,16,0" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                <TextBlock x:Name="HeaderTitle" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock x:Name="HeaderSubtitle" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
            <TextBlock x:Name="HeaderVersion" Grid.Column="2" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
        </Grid>
    </Expander.Header>
    <StackPanel Spacing="12" Margin="0,8,0,0">
        <StackPanel Spacing="4">
            <TextBlock x:Name="YasbSiteLabel" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <HyperlinkButton x:Name="YasbSiteLink" Padding="0" Margin="0"/>
        </StackPanel>
        <StackPanel Spacing="4">
            <TextBlock x:Name="YasbRepoLabel" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <HyperlinkButton x:Name="YasbRepoLink" Padding="0" Margin="0"/>
        </StackPanel>
        <StackPanel Spacing="4">
            <TextBlock x:Name="YasbGuiRepoLabel" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <HyperlinkButton x:Name="YasbGuiRepoLink" Padding="0" Margin="0"/>
        </StackPanel>
        <StackPanel Spacing="4">
            <TextBlock x:Name="VersionLabel" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <TextBlock x:Name="VersionValue" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
        <StackPanel Spacing="4">
            <TextBlock x:Name="CopyrightLabel" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <TextBlock x:Name="CopyrightValue" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
    </StackPanel>
</Expander>
