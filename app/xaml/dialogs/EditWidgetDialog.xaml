<ContentDialog xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                Title="{title}"
                PrimaryButtonText="{save}"
                CloseButtonText="{cancel}"
                DefaultButton="Primary">
  <ContentDialog.Resources>
    <x:Double x:Key="ContentDialogMaxWidth">800</x:Double>
    <x:Double x:Key="ContentDialogMaxHeight">600</x:Double>
  </ContentDialog.Resources>
  <StackPanel Spacing="12" MinWidth="750">
    <TextBlock x:Name="TypeText" Opacity="0.7"/>
    <TextBlock x:Name="OptionsLabel" Style="{{StaticResource BodyStrongTextBlockStyle}}"/>
    <ScrollViewer MaxHeight="400" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
      <TextBox x:Name="OptionsBox" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="200" HorizontalAlignment="Stretch"/>
    </ScrollViewer>
    <Button x:Name="FormatButton" HorizontalAlignment="Left" Content="{format_btn}"/>
  </StackPanel>
</ContentDialog>
